=render :partial => 'shared/header'
%h1 Products
.tab-pane
  .container
    .row
      .span4
      - @products.each do |product|
        %br
        .card{:style => "width: 20rem;"}
          -#%img.card-img-top{:alt => "Card image cap", :src => "..."}/
          .card-block
            %h4= product.title
            %p.card-text= product.description
            %h4= product.price
            -#.progress
            -#  .progress-bar{"aria-valuemax" => "100", "aria-valuemin" => "50", "aria-valuenow" => "25", :role => "progressbar"}
            = link_to 'More details', product_path(product), class: "btn btn-primary"




-#%table
-#  %tr
-#    %th Title
-#    %th Description
-#    %th Price
-#  - @products.each do |product|
-#    %tr
-#      %td= product.title
-#      %td= product.description
-#      %td= product.price
-#      %td= link_to 'Show', product_path(product)
-#      %td= link_to 'Edit', edit_product_path(product)
-#      %td= link_to 'Destroy', product_path(product), method: :delete, data: { confirm: 'Are you sure?' }